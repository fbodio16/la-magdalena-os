{
  "name": "la-magdalena-os",
  "private": true,
  "version": "1.2.2",
  "scripts": {
    "mobile": "cd apps/mobile && npm install && npx expo start --clear",
    "web:local": "cd apps/web && python3 -m http.server 8080",
    "web:deploy": "cd apps/web && npx vercel --prod"
  }
}
